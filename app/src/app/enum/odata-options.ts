export enum ODataOptions {
    Filter = '$filter',
    Top = '$top',
    Skip = '$skip',
    OrderBy = '$orderby',
    Select = '$select',
    Expand = '$expand',
    Count = '$count',
    Search = '$search',
    Format = '$format',
    InlineCount = '$inlinecount',
    SkipToken = '$skiptoken'
}